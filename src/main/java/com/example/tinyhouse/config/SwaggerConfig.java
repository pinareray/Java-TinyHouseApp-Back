package com.example.tinyhouse.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Şu anda özel bir yapılandırma gerekmez,
    // springdoc-openapi-starter-webmvc-ui bağımlılığı
    // otomatik yapılandırmayı sağlar.
}
